// const defaultReaction = "/-heart";

// chrome.runtime.onInstalled.addListener(() => {
//   chrome.storage.sync.set({ defaultReaction });
//   console.log(`Default reaction set to ${defaultReaction}`);
// });